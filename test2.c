#include <stdio.h>
int main (void)
{
    printf("Enter N,v,T");
    int N,v,T,T_;int v[10001],T[10001];
    scanf("%d %d %d %d", &N&v[i]&T[i]&T_[i]);
    
    if(N >=0 && N <= 10000 && v >= 0 && v <= 40)
    {   
        T_[i] = ([4500/(v[i]*1000/3600]+T[i]);

        int min = T_[0];
        printf("%d",T_[i]);
        for(int i=0;i<10;i++)
        {
            printf("%d",T_[i]);
            if(T_[i]<min) min=T_[i];
        }
        printf("%d",min); 

    else
    printf("error");
    return 0;
    } 
}